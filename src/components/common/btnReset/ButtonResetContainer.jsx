import React from "react";
import ButtonReset from "./ButtonReset";

const ButtonTogglePlayContainer = ({ onClick }) => {
  return <ButtonReset onClick={onClick} />;
};

export default ButtonTogglePlayContainer;
